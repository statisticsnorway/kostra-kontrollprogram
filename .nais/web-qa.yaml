apiVersion: nais.io/v1alpha1
kind: Application
metadata:
  labels:
    team: dftjen
  name: qa-kostra-kontrollprogram-web
  namespace: dftjen
spec:
  ingresses:
    - https://kostra-kontrollprogram.test.ssb.no
  port: 8080
  replicas:
    max: 1
    min: 1
  resources:
    requests:
      cpu: 50m
      memory: 160Mi
