name: Run checks

on:
  push:
    branches:
      - master

  pull_request:

permissions:
  contents: read

jobs:
  build:
    uses: ./.github/workflows/build-template.yml
    secrets: inherit
    with:
      gradle-targets: check